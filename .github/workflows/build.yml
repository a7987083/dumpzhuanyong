name: build-adtrace-v2

on:
  push:
    branches: [main, 'dev/**', 'feature/**']
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Xcode version
        run: xcodebuild -version
      - name: Source checks
        run: make source-check
      - name: Build
        run: make clean all
      - name: Verify Mach-O
        run: make verify
      - name: SHA-256
        run: shasum -a 256 build/DumpZhuanYongAdTraceV2.dylib | tee build/SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: DumpZhuanYongAdTraceV2-dev
          path: |
            build/DumpZhuanYongAdTraceV2.dylib
            build/SHA256SUMS.txt
          if-no-files-found: error
